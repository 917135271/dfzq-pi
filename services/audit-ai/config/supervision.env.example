OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.deepseek.com
OPENAI_MODEL=deepseek-v4-flash
AUDIT_AI_INTERNAL_TOKEN=

# Enable after installing MinerU and configuring its local model weights.
# PIPELINE_OCR_BACKEND=mineru
# Optional writable ASCII-only cache for Windows FastText model paths.
# PIPELINE_OCR_MODEL_CACHE=C:/audit-ai-ocr-models
