# 监督提取实施状态

## 已完成

- [x] Java 上传元数据、组织目录和字段规则的独立严格契约。
- [x] 复用现有已入库 IR/PG chunk，逐段和逐表格行保留证据；不重跑 OCR 或新建上传管线。
- [x] 模型提取接缝、默认关闭、全量证据分组、摘录/规则/字段/组织范围验证。
- [x] 内部令牌接口挂载、版本状态校验、提取期间内容变化复验。
- [x] 定向测试与 Pi 上传适配的离线交叉验证。

## 验证记录（2026-09-04）

`pytest -q pipeline/tests/test_supervision_extraction.py query/tests/test_supervision_api.py query/tests/test_api_boundary.py`：42 passed。新代码 Ruff check 通过，git diff --check 通过。仅原有 pymilvus/pkg_resources 弃用提示。

使用真实 LightParser 解析临时生成的 DOCX（含表格）并通过本地 ObjectStore 读取证据；PG 状态/查询与模型响应为替身，未调用真实数据库或付费模型。
跨仓离线验证：audit-ai 生成 1 份 indexed 资料投影、3 条证据、1 个未确认候选；Pi 的实际上传适配器和快照函数接受该投影并纳入分析期间。
现有虚拟环境 Python 3.12.7，Ruff 目标仍为 py311；未宣称已经通过 Python 3.11 运行验证。

## 决策及边界

### 2026-09-04 真实 PDF 提取补测

- 从上交所下载原始《监管措施决定书〔2024〕60号》PDF（3 页），LightParser 解析 60 个块。首次真实 DeepSeek V4 Flash 调用返回缩写证据 ID 和跨行拼接 quote，被严格验证拒绝，未当作成功结果。
- 监督证据层补充 PAGE_TEXT 同页连续文本窗口，定位字段由后端派生，提示词要求完整证据 ID 和保留换行。存储 IR、通用解析器、DB schema 未改。
- 同一 PDF、同一实际 Pi 监管规则复测：3 个证据窗口、1 个事实候选、9 个模型字段及 1 个派生定位字段，EXTRACTED；模型调用约 100.42 秒。字段引用逐字校验通过，正式日期、机构、相对整改期限与原件核对；未虚构整改措施、状态或结果。
- 当时发现的质量限制：第 2 页代持事项的详细经过未充分进入 issueDescription。该项已在下述跨页补全验证中修复；仍不能直接用候选数统计问题数。
- 独立 OCR 验证使用先前按公开文字重建的扫描 PDF，MinerU 解析得到 8 块，约 61.47 秒；该件不是甲方或监管机构发布的原始 PDF，不与上交所原件测试混称。
- 44 项监督提取/路由/边界定向测试通过，Ruff 通过。调用的是实际解析器和 prepare_extraction，未经过 Java 上传、HTTP 接口、PG/Milvus 状态核验及报告生成。测试的 API 超时为 240 秒，不能据此宣称默认服务配置已满足长耗时模型调用。

### 2026-09-04 跨页经过与多引用补全

- 新增可选 supportingEvidence，主引用与所有补充引用逐条验证；派生定位涵盖全部引用。旧单引用结构与事实 ID 有兼容回归测试。
- 提示词从“单引用支持不了则省略”改为“明确同一事项的具体经过和结论共同保留，各附依据”，没有加入本次样本的正确答案。
- 同一原始 PDF/规则/模型：第一轮返回非法 JSON，拒绝且不归因为已确认的长度问题；第二轮正常停止（finishReason=stop），真实响应约 195.08 秒，已保留 2019 年协议、2023 年 5 月与 7 月结清时点、申报后结清、未披露事项、7.97%表决权及保荐责任结论。
- 第二轮响应仍有 6 处换行位置偏差。新增 PAGE_TEXT 唯一精确字符匹配恢复原折行，保存恢复记录；改字、漏字、空格差异、数字变化、多处匹配和非 PAGE_TEXT 继续拒绝。用未修改的真实响应回放，核验模型输入和提示词与原调用一致后通过最终校验；未把回放说成额外模型调用。
- 最终 1 个事实候选、10 个模型字段和 1 个派生定位；issueDescription 同时引用第 2、3 页。8 项本样本细节核对通过（关键词检查辅助人工原件核对，不作为通用准确率）；没有生成整改措施、状态或结果。
- 定向监督/接口/边界测试 57 passed，Ruff 和 git diff --check 通过。未改 Java/Pi 产品代码、数据库、模型开关或通用解析器。
- 边界：跨组或跨资料的事项融合仍由后续业务消费层处理，不能因内容相似强行合并；单样本实测不能替代真实批量材料的完整性评测。模型仍有耗时和生成稳定性风险，失败不伪装成功。

- 上传字段的持久化在 Java。Python 接收本平台版本元数据，不重复建立业务资料库、不关联甲方记录。
- 功能入口放在 S0–S5 之后，因为 Pi 只消费已入库版本；用现成 IR 获取完整正文，避免用检索 TopK 替代统计资料范围。
- 事实候选统一保留 PENDING_REVIEW：结构/摘录校验无法证明语义判断正确。资料范围无需用户确认，与业务事实确认规则分开。
- EVIDENCE_READY 明确表示未执行语义提取；模型失败返回错误，不能用空 facts 伪装无问题。
- 文档/解析内容/索引变化后重新获取快照；不在抽取过程中隐式修改源文本或状态。

## 尚需生产联调

Java 上传元数据/版本映射和授权调用；部署模型配置与真实材料提取评测；facts 到业务问题、整改、问责记录的分类和确认消费；真实 PG/Milvus/Java 全链路。本轮没有修改这些 Java 实现，也未宣称报告自动生成全链路验收完成。
